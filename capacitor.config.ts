import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.digital_dynamometer',
  appName: 'digital_dynamometer',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
